package com.zps.AWT;

import java.awt.*;

public class CardLayout {
    public static void main(String[] args) {
        //跟扑克差不多，
        /*
        * first(容器名)显示第一个组件
        * last(容器名)显示最后一个组件
        * previous(容器名)显示前一个组件
        * next(容器名)显示下一个组件
        * */



    }
}
